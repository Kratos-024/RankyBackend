// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init


generator client {
  provider = "prisma-client-js"
  output = "../../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://neondb_owner:npg_RQw4Djml5BfG@ep-morning-sun-a8h45sr5-pooler.eastus2.azure.neon.tech/neondb?sslmode=require"
}

model Streak {
  id String @unique() @default(uuid())
  uniqueToken String  @unique()
  date String
  username String @unique()
  streak Int  
}

model UserAccount {
  id String @unique() @default(uuid())
  name String
  uniqueToken String  @unique()
  username String @unique()
  email String
}
model Languages {
  id String @unique() @default(uuid())
  language String[] 
}


model UserDailyStats {
  id                String  @id @default(uuid()) 
  uniqueToken       String
  date              String
  totalTimeSeconds  Decimal
  totalTimeMinutes  Decimal
  totalWords        Int
  totalLines        Int
  languages         String[]
  earlyMorning      String
  lateNight         String

   @@unique([uniqueToken, date]) 
}


model gitStreak{
  unqiueToken String @unique
  gitDate String
  count Int
  level Int
}