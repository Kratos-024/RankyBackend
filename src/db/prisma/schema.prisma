// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init


generator client {
  provider = "prisma-client-js"
  output = "../../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://ranky_owner:npg_4Q6LlnqhSGvE@ep-steep-hall-a80y0y04-pooler.eastus2.azure.neon.tech/ranky?sslmode=require"
}

model Streak {
  id String @unique() @default(uuid())
  uniqueToken String  @unique()
  date String
  username String @unique()
  streak Int  
}

model UserAccount {
  id String @unique() @default(uuid())
  name String
  uniqueToken String?  @unique()
  username String? @unique()
  avatar_url String?
  email String @unique()
  refreshToken String?
  accessToken String?
  bio String?
  twitter_username String?
}
model Languages {
  id String @unique() @default(uuid())
  language String[] 
}


model UserDailyStats {
  id                String  @id @default(uuid()) 
  uniqueToken       String
  date              String
  totalTimeSeconds  Decimal
  totalTimeMinutes  Decimal
  totalWords        Int
  totalLines        Int
  languages         String[]
  earlyMorning      String
  lateNight         String

   @@unique([uniqueToken, date]) 
}


model gitStreak{
  unqiueToken String @unique
  gitDate String
  count Int
  level Int
}